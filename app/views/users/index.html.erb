<% if flash[:notice] %>
    <%= flash[:notice] %>
<% end %>

<%= link_to '新規会員登録はこちら', new_user_path %></br>

<%= link_to 'ログインはこちら', top_login_move_path %>


<h2>登録ユーザー一覧</h2>

<% @users.each do |user| %>
    <div>
        <%= user.uid %>
        <%= link_to '削除', "/users/#{user.id}", data: {turbo_method: :delete} %>
    </div>
<% end %>
